// toString 
let num = 10;
const a =num.toString(2);
console.log(a)

// Math 
/**
 * ceil : 올림 
 * floor : 내림
 * round : 반올림 
 * toFixed() : 넣은 숫자만큼 소수점 갯수 조절
 * isNaN() 
 * parseInt : 10px -> 10  ( 앞에서부터 잘라서 숫자 반환 )
 *            f3 -> NaN
 * Math.random() : 0~1 사이 소수 반환 
 * 
 */